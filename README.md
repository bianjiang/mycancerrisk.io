facebook Graph to get email
outh to outhcenticate and get access token

new user log into userpage to update info
old user log into homepage

# setup

* `pip install -r requirements.txt`
* `cd static`
* `bower install`